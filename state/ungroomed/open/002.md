---
id: U-002
milestone: ungroomed
sprint: TODO
priority: medium
---

No unit test covers the case where an allowed tool exits with a non-zero code (other than via timeout).

**Recommendation:** Add a test mocking `spawn` to emit `exit` with a non-zero code and verify `executeTool` rejects with an error containing the code.
