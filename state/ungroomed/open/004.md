---
id: U-004
milestone: ungroomed
sprint: TODO
priority: low
---

toolRunner.checkAllowlist does a simple `includes()`; a malicious entry like `../bin/ls` could circumvent if allowlist is manipulated.

**Recommendation:** Canonicalise/normalize `toolName` before comparison or document that allowlist entries must be exact binaries.
